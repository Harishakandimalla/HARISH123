#include <stdio.h>

int main() {
    char str1[100], str2[100], str3[200];
    int i, j;
    printf("Input the first string: ");
    fgets(str1, 100, stdin);
    printf("Input the second string: ");
    fgets(str2, 100, stdin);
    i = 0;
    while (str1[i] != '\0') {
        str3[i] = str1[i];
        i++;
    }
    j = 0;
    while (str2[j] != '\0') {
        str3[i] = str2[j];
        i++;
        j++;
    }
    str3[i] = '\0';
    printf("The concatenated string is: %s", str3);
    return 0;
}
output:
Input the first string: Saveetha
Input the second string: College
The concatenated string is: Saveetha College
